type: object
required: [embedding,index]
properties:
  embedding:
    oneOf:
      - type: string
      - type: array
        items: {type: number}
  index: {type: integer}
  object: {type: string, enum: [embedding]}
