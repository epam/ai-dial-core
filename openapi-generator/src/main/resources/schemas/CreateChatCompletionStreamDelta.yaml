type: object
properties:
  role:
    type: string
    enum:
      - assistant
  content:
    type: string
  function_call:
    $ref: '#/components/schemas/FunctionCall'
  tool_calls:
    type: array
    items:
      $ref: '#/components/schemas/StreamToolCall'
  custom_content:
    $ref: '#/components/schemas/CustomContent'
